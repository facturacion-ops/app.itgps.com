# FASE 16.5 V7 — Guardado definitivo

La cotización se confirma en SQLite antes de registrar auditoría. El API devuelve la fila creada y el frontend la incorpora inmediatamente al listado. Se conserva el diseño aprobado de V4 y no se requiere db:init.
