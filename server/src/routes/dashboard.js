import {Router} from "express";import {db} from "../db/database.js";import {requireAuth} from "../middleware/auth.js";const r=Router();r.get("/",requireAuth,(q,s)=>{const c=t=>db.prepare(`SELECT COUNT(*) n FROM ${t} WHERE empresa_id=?`).get(q.user.empresa_id).n;s.json({usuario:q.user,kpis:{prospectos:c("prospectos"),clientes:c("clientes"),equipos:c("equipos"),sim:c("sim_cards")}})});export default r;
